<%= render partial: "layouts/errors", locals: {record: comment} %>

<div class="form">
<%= form_for(comment) do |f| %>
    <%= f.label :content %><br>
    <%= f.text_area :content %><br>
    <%= f.submit %>
  <% end %> 
</div>